# Notes

## Active

**implement filtering (e.g., Kalmann filter) for smoother robot movement.**

**my naming system**
shoulder(can rotate)
elbow_base(bends 180 degrees)
wist(rotates, 90 degreen in flexation,
    90 in extention, 90 for radial deviation )

## Archived

**yolo26n-pose.pt** # the pose model for tracking arm movemnt and pose (x,y cords for this)
Switched to ArUco — no longer using YOLO for pose.

**Convert BGR frames to RGB for MediaPipe processing.**
Not using MediaPipe anymore.
